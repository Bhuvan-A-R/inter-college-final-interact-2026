export const metadata = {
  title: "Events",
  description: "Participate in events of GAT INTERACT.",
};

export default function EventsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
